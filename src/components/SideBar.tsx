import React from 'react'

type Props = {}

const SideBar: React.FC<Props> = (props) => {
  return (
    <aside className="bg-gray-600 min-h-screen w-20">
      <nav></nav>
    </aside>
  )
}

export default SideBar
